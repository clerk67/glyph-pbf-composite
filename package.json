{
  "name": "glyph-pbf-composite",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "generate": "npm run generate:sans && npm run generate:serif",
    "generate:sans": "node dist IBMPlexSans",
    "generate:serif": "node dist IBMPlexSerif",
    "prepare": "npm run build"
  },
  "type": "module",
  "dependencies": {
    "protocol-buffers": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "typescript": "^5.7.3"
  }
}
